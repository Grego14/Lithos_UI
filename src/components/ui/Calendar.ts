/**
 * @fileoverview Lithos UI calendar public entry point (barrel file).
 * - Re-exports the Calendar component and types for consumer consumption.
 */

export { Calendar } from './calendar/Calendar'
export type * from './calendar/calendar.types'
